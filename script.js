const service=document.getElementById('service');
document.querySelectorAll('.price-card button').forEach(btn=>btn.addEventListener('click',()=>{service.value=btn.dataset.service;document.getElementById('order').scrollIntoView({behavior:'smooth'});}));
document.getElementById('orderForm').addEventListener('submit',e=>{e.preventDefault();const data={name:document.getElementById('name').value,contactType:document.getElementById('contactType').value,contact:document.getElementById('contact').value,service:service.value,details:document.getElementById('details').value,materials:document.getElementById('materials').value};const text=`【能量工作室｜委託需求】\n稱呼：${data.name}\n聯絡：${data.contactType} / ${data.contact}\n項目：${data.service}\n需求：${data.details}\n素材：${data.materials||'未提供'}`;navigator.clipboard?.writeText(text);document.getElementById('formStatus').textContent='需求已整理完成（目前為展示版，資料已複製到剪貼簿；正式付款／上傳系統下一階段接入）。';});
